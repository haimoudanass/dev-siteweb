#!/bin/sh
# Remove Cursor co-author trailer so commits show only your identity.
msg="$1"
[ -z "$msg" ] || [ ! -f "$msg" ] && exit 0
tmp="${msg}.strip.$$"
grep -vi 'Co-authored-by:.*cursoragent' "$msg" >"$tmp" && mv -f "$tmp" "$msg"
exit 0
